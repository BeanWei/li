// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/BeanWei/li/li-engine/contrib/liflow/ent/flowinstancedata"
	"github.com/BeanWei/li/li-engine/contrib/liflow/ent/predicate"
)

// FlowInstanceDataQuery is the builder for querying FlowInstanceData entities.
type FlowInstanceDataQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.FlowInstanceData
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the FlowInstanceDataQuery builder.
func (fidq *FlowInstanceDataQuery) Where(ps ...predicate.FlowInstanceData) *FlowInstanceDataQuery {
	fidq.predicates = append(fidq.predicates, ps...)
	return fidq
}

// Limit adds a limit step to the query.
func (fidq *FlowInstanceDataQuery) Limit(limit int) *FlowInstanceDataQuery {
	fidq.limit = &limit
	return fidq
}

// Offset adds an offset step to the query.
func (fidq *FlowInstanceDataQuery) Offset(offset int) *FlowInstanceDataQuery {
	fidq.offset = &offset
	return fidq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (fidq *FlowInstanceDataQuery) Unique(unique bool) *FlowInstanceDataQuery {
	fidq.unique = &unique
	return fidq
}

// Order adds an order step to the query.
func (fidq *FlowInstanceDataQuery) Order(o ...OrderFunc) *FlowInstanceDataQuery {
	fidq.order = append(fidq.order, o...)
	return fidq
}

// First returns the first FlowInstanceData entity from the query.
// Returns a *NotFoundError when no FlowInstanceData was found.
func (fidq *FlowInstanceDataQuery) First(ctx context.Context) (*FlowInstanceData, error) {
	nodes, err := fidq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{flowinstancedata.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (fidq *FlowInstanceDataQuery) FirstX(ctx context.Context) *FlowInstanceData {
	node, err := fidq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first FlowInstanceData ID from the query.
// Returns a *NotFoundError when no FlowInstanceData ID was found.
func (fidq *FlowInstanceDataQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = fidq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{flowinstancedata.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (fidq *FlowInstanceDataQuery) FirstIDX(ctx context.Context) string {
	id, err := fidq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single FlowInstanceData entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one FlowInstanceData entity is found.
// Returns a *NotFoundError when no FlowInstanceData entities are found.
func (fidq *FlowInstanceDataQuery) Only(ctx context.Context) (*FlowInstanceData, error) {
	nodes, err := fidq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{flowinstancedata.Label}
	default:
		return nil, &NotSingularError{flowinstancedata.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (fidq *FlowInstanceDataQuery) OnlyX(ctx context.Context) *FlowInstanceData {
	node, err := fidq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only FlowInstanceData ID in the query.
// Returns a *NotSingularError when more than one FlowInstanceData ID is found.
// Returns a *NotFoundError when no entities are found.
func (fidq *FlowInstanceDataQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = fidq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{flowinstancedata.Label}
	default:
		err = &NotSingularError{flowinstancedata.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (fidq *FlowInstanceDataQuery) OnlyIDX(ctx context.Context) string {
	id, err := fidq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of FlowInstanceDataSlice.
func (fidq *FlowInstanceDataQuery) All(ctx context.Context) ([]*FlowInstanceData, error) {
	if err := fidq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return fidq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (fidq *FlowInstanceDataQuery) AllX(ctx context.Context) []*FlowInstanceData {
	nodes, err := fidq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of FlowInstanceData IDs.
func (fidq *FlowInstanceDataQuery) IDs(ctx context.Context) ([]string, error) {
	var ids []string
	if err := fidq.Select(flowinstancedata.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (fidq *FlowInstanceDataQuery) IDsX(ctx context.Context) []string {
	ids, err := fidq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (fidq *FlowInstanceDataQuery) Count(ctx context.Context) (int, error) {
	if err := fidq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return fidq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (fidq *FlowInstanceDataQuery) CountX(ctx context.Context) int {
	count, err := fidq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (fidq *FlowInstanceDataQuery) Exist(ctx context.Context) (bool, error) {
	if err := fidq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return fidq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (fidq *FlowInstanceDataQuery) ExistX(ctx context.Context) bool {
	exist, err := fidq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the FlowInstanceDataQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (fidq *FlowInstanceDataQuery) Clone() *FlowInstanceDataQuery {
	if fidq == nil {
		return nil
	}
	return &FlowInstanceDataQuery{
		config:     fidq.config,
		limit:      fidq.limit,
		offset:     fidq.offset,
		order:      append([]OrderFunc{}, fidq.order...),
		predicates: append([]predicate.FlowInstanceData{}, fidq.predicates...),
		// clone intermediate query.
		sql:    fidq.sql.Clone(),
		path:   fidq.path,
		unique: fidq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt int64 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.FlowInstanceData.Query().
//		GroupBy(flowinstancedata.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (fidq *FlowInstanceDataQuery) GroupBy(field string, fields ...string) *FlowInstanceDataGroupBy {
	grbuild := &FlowInstanceDataGroupBy{config: fidq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := fidq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return fidq.sqlQuery(ctx), nil
	}
	grbuild.label = flowinstancedata.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt int64 `json:"created_at,omitempty"`
//	}
//
//	client.FlowInstanceData.Query().
//		Select(flowinstancedata.FieldCreatedAt).
//		Scan(ctx, &v)
//
func (fidq *FlowInstanceDataQuery) Select(fields ...string) *FlowInstanceDataSelect {
	fidq.fields = append(fidq.fields, fields...)
	selbuild := &FlowInstanceDataSelect{FlowInstanceDataQuery: fidq}
	selbuild.label = flowinstancedata.Label
	selbuild.flds, selbuild.scan = &fidq.fields, selbuild.Scan
	return selbuild
}

func (fidq *FlowInstanceDataQuery) prepareQuery(ctx context.Context) error {
	for _, f := range fidq.fields {
		if !flowinstancedata.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if fidq.path != nil {
		prev, err := fidq.path(ctx)
		if err != nil {
			return err
		}
		fidq.sql = prev
	}
	return nil
}

func (fidq *FlowInstanceDataQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*FlowInstanceData, error) {
	var (
		nodes = []*FlowInstanceData{}
		_spec = fidq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*FlowInstanceData).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &FlowInstanceData{config: fidq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, fidq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}

	return nodes, nil
}

func (fidq *FlowInstanceDataQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := fidq.querySpec()
	_spec.Node.Columns = fidq.fields
	if len(fidq.fields) > 0 {
		_spec.Unique = fidq.unique != nil && *fidq.unique
	}
	return sqlgraph.CountNodes(ctx, fidq.driver, _spec)
}

func (fidq *FlowInstanceDataQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := fidq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (fidq *FlowInstanceDataQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   flowinstancedata.Table,
			Columns: flowinstancedata.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeString,
				Column: flowinstancedata.FieldID,
			},
		},
		From:   fidq.sql,
		Unique: true,
	}
	if unique := fidq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := fidq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, flowinstancedata.FieldID)
		for i := range fields {
			if fields[i] != flowinstancedata.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := fidq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := fidq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := fidq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := fidq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (fidq *FlowInstanceDataQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(fidq.driver.Dialect())
	t1 := builder.Table(flowinstancedata.Table)
	columns := fidq.fields
	if len(columns) == 0 {
		columns = flowinstancedata.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if fidq.sql != nil {
		selector = fidq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if fidq.unique != nil && *fidq.unique {
		selector.Distinct()
	}
	for _, p := range fidq.predicates {
		p(selector)
	}
	for _, p := range fidq.order {
		p(selector)
	}
	if offset := fidq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := fidq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// FlowInstanceDataGroupBy is the group-by builder for FlowInstanceData entities.
type FlowInstanceDataGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (fidgb *FlowInstanceDataGroupBy) Aggregate(fns ...AggregateFunc) *FlowInstanceDataGroupBy {
	fidgb.fns = append(fidgb.fns, fns...)
	return fidgb
}

// Scan applies the group-by query and scans the result into the given value.
func (fidgb *FlowInstanceDataGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := fidgb.path(ctx)
	if err != nil {
		return err
	}
	fidgb.sql = query
	return fidgb.sqlScan(ctx, v)
}

func (fidgb *FlowInstanceDataGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range fidgb.fields {
		if !flowinstancedata.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := fidgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fidgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (fidgb *FlowInstanceDataGroupBy) sqlQuery() *sql.Selector {
	selector := fidgb.sql.Select()
	aggregation := make([]string, 0, len(fidgb.fns))
	for _, fn := range fidgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(fidgb.fields)+len(fidgb.fns))
		for _, f := range fidgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(fidgb.fields...)...)
}

// FlowInstanceDataSelect is the builder for selecting fields of FlowInstanceData entities.
type FlowInstanceDataSelect struct {
	*FlowInstanceDataQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (fids *FlowInstanceDataSelect) Scan(ctx context.Context, v interface{}) error {
	if err := fids.prepareQuery(ctx); err != nil {
		return err
	}
	fids.sql = fids.FlowInstanceDataQuery.sqlQuery(ctx)
	return fids.sqlScan(ctx, v)
}

func (fids *FlowInstanceDataSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := fids.sql.Query()
	if err := fids.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
