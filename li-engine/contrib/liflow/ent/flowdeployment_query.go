// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/BeanWei/li/li-engine/contrib/liflow/ent/flowdefinition"
	"github.com/BeanWei/li/li-engine/contrib/liflow/ent/flowdeployment"
	"github.com/BeanWei/li/li-engine/contrib/liflow/ent/flowinstance"
	"github.com/BeanWei/li/li-engine/contrib/liflow/ent/predicate"
)

// FlowDeploymentQuery is the builder for querying FlowDeployment entities.
type FlowDeploymentQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.FlowDeployment
	// eager-loading edges.
	withFlowDefinition *FlowDefinitionQuery
	withFlowInstances  *FlowInstanceQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the FlowDeploymentQuery builder.
func (fdq *FlowDeploymentQuery) Where(ps ...predicate.FlowDeployment) *FlowDeploymentQuery {
	fdq.predicates = append(fdq.predicates, ps...)
	return fdq
}

// Limit adds a limit step to the query.
func (fdq *FlowDeploymentQuery) Limit(limit int) *FlowDeploymentQuery {
	fdq.limit = &limit
	return fdq
}

// Offset adds an offset step to the query.
func (fdq *FlowDeploymentQuery) Offset(offset int) *FlowDeploymentQuery {
	fdq.offset = &offset
	return fdq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (fdq *FlowDeploymentQuery) Unique(unique bool) *FlowDeploymentQuery {
	fdq.unique = &unique
	return fdq
}

// Order adds an order step to the query.
func (fdq *FlowDeploymentQuery) Order(o ...OrderFunc) *FlowDeploymentQuery {
	fdq.order = append(fdq.order, o...)
	return fdq
}

// QueryFlowDefinition chains the current query on the "flow_definition" edge.
func (fdq *FlowDeploymentQuery) QueryFlowDefinition() *FlowDefinitionQuery {
	query := &FlowDefinitionQuery{config: fdq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := fdq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := fdq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(flowdeployment.Table, flowdeployment.FieldID, selector),
			sqlgraph.To(flowdefinition.Table, flowdefinition.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, flowdeployment.FlowDefinitionTable, flowdeployment.FlowDefinitionColumn),
		)
		fromU = sqlgraph.SetNeighbors(fdq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryFlowInstances chains the current query on the "flow_instances" edge.
func (fdq *FlowDeploymentQuery) QueryFlowInstances() *FlowInstanceQuery {
	query := &FlowInstanceQuery{config: fdq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := fdq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := fdq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(flowdeployment.Table, flowdeployment.FieldID, selector),
			sqlgraph.To(flowinstance.Table, flowinstance.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, flowdeployment.FlowInstancesTable, flowdeployment.FlowInstancesColumn),
		)
		fromU = sqlgraph.SetNeighbors(fdq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first FlowDeployment entity from the query.
// Returns a *NotFoundError when no FlowDeployment was found.
func (fdq *FlowDeploymentQuery) First(ctx context.Context) (*FlowDeployment, error) {
	nodes, err := fdq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{flowdeployment.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (fdq *FlowDeploymentQuery) FirstX(ctx context.Context) *FlowDeployment {
	node, err := fdq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first FlowDeployment ID from the query.
// Returns a *NotFoundError when no FlowDeployment ID was found.
func (fdq *FlowDeploymentQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = fdq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{flowdeployment.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (fdq *FlowDeploymentQuery) FirstIDX(ctx context.Context) string {
	id, err := fdq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single FlowDeployment entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one FlowDeployment entity is found.
// Returns a *NotFoundError when no FlowDeployment entities are found.
func (fdq *FlowDeploymentQuery) Only(ctx context.Context) (*FlowDeployment, error) {
	nodes, err := fdq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{flowdeployment.Label}
	default:
		return nil, &NotSingularError{flowdeployment.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (fdq *FlowDeploymentQuery) OnlyX(ctx context.Context) *FlowDeployment {
	node, err := fdq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only FlowDeployment ID in the query.
// Returns a *NotSingularError when more than one FlowDeployment ID is found.
// Returns a *NotFoundError when no entities are found.
func (fdq *FlowDeploymentQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = fdq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{flowdeployment.Label}
	default:
		err = &NotSingularError{flowdeployment.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (fdq *FlowDeploymentQuery) OnlyIDX(ctx context.Context) string {
	id, err := fdq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of FlowDeployments.
func (fdq *FlowDeploymentQuery) All(ctx context.Context) ([]*FlowDeployment, error) {
	if err := fdq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return fdq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (fdq *FlowDeploymentQuery) AllX(ctx context.Context) []*FlowDeployment {
	nodes, err := fdq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of FlowDeployment IDs.
func (fdq *FlowDeploymentQuery) IDs(ctx context.Context) ([]string, error) {
	var ids []string
	if err := fdq.Select(flowdeployment.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (fdq *FlowDeploymentQuery) IDsX(ctx context.Context) []string {
	ids, err := fdq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (fdq *FlowDeploymentQuery) Count(ctx context.Context) (int, error) {
	if err := fdq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return fdq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (fdq *FlowDeploymentQuery) CountX(ctx context.Context) int {
	count, err := fdq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (fdq *FlowDeploymentQuery) Exist(ctx context.Context) (bool, error) {
	if err := fdq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return fdq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (fdq *FlowDeploymentQuery) ExistX(ctx context.Context) bool {
	exist, err := fdq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the FlowDeploymentQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (fdq *FlowDeploymentQuery) Clone() *FlowDeploymentQuery {
	if fdq == nil {
		return nil
	}
	return &FlowDeploymentQuery{
		config:             fdq.config,
		limit:              fdq.limit,
		offset:             fdq.offset,
		order:              append([]OrderFunc{}, fdq.order...),
		predicates:         append([]predicate.FlowDeployment{}, fdq.predicates...),
		withFlowDefinition: fdq.withFlowDefinition.Clone(),
		withFlowInstances:  fdq.withFlowInstances.Clone(),
		// clone intermediate query.
		sql:    fdq.sql.Clone(),
		path:   fdq.path,
		unique: fdq.unique,
	}
}

// WithFlowDefinition tells the query-builder to eager-load the nodes that are connected to
// the "flow_definition" edge. The optional arguments are used to configure the query builder of the edge.
func (fdq *FlowDeploymentQuery) WithFlowDefinition(opts ...func(*FlowDefinitionQuery)) *FlowDeploymentQuery {
	query := &FlowDefinitionQuery{config: fdq.config}
	for _, opt := range opts {
		opt(query)
	}
	fdq.withFlowDefinition = query
	return fdq
}

// WithFlowInstances tells the query-builder to eager-load the nodes that are connected to
// the "flow_instances" edge. The optional arguments are used to configure the query builder of the edge.
func (fdq *FlowDeploymentQuery) WithFlowInstances(opts ...func(*FlowInstanceQuery)) *FlowDeploymentQuery {
	query := &FlowInstanceQuery{config: fdq.config}
	for _, opt := range opts {
		opt(query)
	}
	fdq.withFlowInstances = query
	return fdq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt int64 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.FlowDeployment.Query().
//		GroupBy(flowdeployment.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (fdq *FlowDeploymentQuery) GroupBy(field string, fields ...string) *FlowDeploymentGroupBy {
	grbuild := &FlowDeploymentGroupBy{config: fdq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := fdq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return fdq.sqlQuery(ctx), nil
	}
	grbuild.label = flowdeployment.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt int64 `json:"created_at,omitempty"`
//	}
//
//	client.FlowDeployment.Query().
//		Select(flowdeployment.FieldCreatedAt).
//		Scan(ctx, &v)
//
func (fdq *FlowDeploymentQuery) Select(fields ...string) *FlowDeploymentSelect {
	fdq.fields = append(fdq.fields, fields...)
	selbuild := &FlowDeploymentSelect{FlowDeploymentQuery: fdq}
	selbuild.label = flowdeployment.Label
	selbuild.flds, selbuild.scan = &fdq.fields, selbuild.Scan
	return selbuild
}

func (fdq *FlowDeploymentQuery) prepareQuery(ctx context.Context) error {
	for _, f := range fdq.fields {
		if !flowdeployment.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if fdq.path != nil {
		prev, err := fdq.path(ctx)
		if err != nil {
			return err
		}
		fdq.sql = prev
	}
	return nil
}

func (fdq *FlowDeploymentQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*FlowDeployment, error) {
	var (
		nodes       = []*FlowDeployment{}
		_spec       = fdq.querySpec()
		loadedTypes = [2]bool{
			fdq.withFlowDefinition != nil,
			fdq.withFlowInstances != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*FlowDeployment).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &FlowDeployment{config: fdq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, fdq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}

	if query := fdq.withFlowDefinition; query != nil {
		ids := make([]string, 0, len(nodes))
		nodeids := make(map[string][]*FlowDeployment)
		for i := range nodes {
			fk := nodes[i].FlowDefinitionID
			if _, ok := nodeids[fk]; !ok {
				ids = append(ids, fk)
			}
			nodeids[fk] = append(nodeids[fk], nodes[i])
		}
		query.Where(flowdefinition.IDIn(ids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := nodeids[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "flow_definition_id" returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.FlowDefinition = n
			}
		}
	}

	if query := fdq.withFlowInstances; query != nil {
		fks := make([]driver.Value, 0, len(nodes))
		nodeids := make(map[string]*FlowDeployment)
		for i := range nodes {
			fks = append(fks, nodes[i].ID)
			nodeids[nodes[i].ID] = nodes[i]
			nodes[i].Edges.FlowInstances = []*FlowInstance{}
		}
		query.Where(predicate.FlowInstance(func(s *sql.Selector) {
			s.Where(sql.InValues(flowdeployment.FlowInstancesColumn, fks...))
		}))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			fk := n.FlowDeploymentID
			node, ok := nodeids[fk]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "flow_deployment_id" returned %v for node %v`, fk, n.ID)
			}
			node.Edges.FlowInstances = append(node.Edges.FlowInstances, n)
		}
	}

	return nodes, nil
}

func (fdq *FlowDeploymentQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := fdq.querySpec()
	_spec.Node.Columns = fdq.fields
	if len(fdq.fields) > 0 {
		_spec.Unique = fdq.unique != nil && *fdq.unique
	}
	return sqlgraph.CountNodes(ctx, fdq.driver, _spec)
}

func (fdq *FlowDeploymentQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := fdq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (fdq *FlowDeploymentQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   flowdeployment.Table,
			Columns: flowdeployment.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeString,
				Column: flowdeployment.FieldID,
			},
		},
		From:   fdq.sql,
		Unique: true,
	}
	if unique := fdq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := fdq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, flowdeployment.FieldID)
		for i := range fields {
			if fields[i] != flowdeployment.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := fdq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := fdq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := fdq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := fdq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (fdq *FlowDeploymentQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(fdq.driver.Dialect())
	t1 := builder.Table(flowdeployment.Table)
	columns := fdq.fields
	if len(columns) == 0 {
		columns = flowdeployment.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if fdq.sql != nil {
		selector = fdq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if fdq.unique != nil && *fdq.unique {
		selector.Distinct()
	}
	for _, p := range fdq.predicates {
		p(selector)
	}
	for _, p := range fdq.order {
		p(selector)
	}
	if offset := fdq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := fdq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// FlowDeploymentGroupBy is the group-by builder for FlowDeployment entities.
type FlowDeploymentGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (fdgb *FlowDeploymentGroupBy) Aggregate(fns ...AggregateFunc) *FlowDeploymentGroupBy {
	fdgb.fns = append(fdgb.fns, fns...)
	return fdgb
}

// Scan applies the group-by query and scans the result into the given value.
func (fdgb *FlowDeploymentGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := fdgb.path(ctx)
	if err != nil {
		return err
	}
	fdgb.sql = query
	return fdgb.sqlScan(ctx, v)
}

func (fdgb *FlowDeploymentGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range fdgb.fields {
		if !flowdeployment.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := fdgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fdgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (fdgb *FlowDeploymentGroupBy) sqlQuery() *sql.Selector {
	selector := fdgb.sql.Select()
	aggregation := make([]string, 0, len(fdgb.fns))
	for _, fn := range fdgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(fdgb.fields)+len(fdgb.fns))
		for _, f := range fdgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(fdgb.fields...)...)
}

// FlowDeploymentSelect is the builder for selecting fields of FlowDeployment entities.
type FlowDeploymentSelect struct {
	*FlowDeploymentQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (fds *FlowDeploymentSelect) Scan(ctx context.Context, v interface{}) error {
	if err := fds.prepareQuery(ctx); err != nil {
		return err
	}
	fds.sql = fds.FlowDeploymentQuery.sqlQuery(ctx)
	return fds.sqlScan(ctx, v)
}

func (fds *FlowDeploymentSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := fds.sql.Query()
	if err := fds.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
